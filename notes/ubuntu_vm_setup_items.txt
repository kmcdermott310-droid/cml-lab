For CML bootstrap:

#cloud-config
hostname: cisco
manage_etc_hosts: True
system_info:
  default_user:
    name: cisco
password: cisco
chpasswd: { expire: False }
ssh_pwauth: True
ssh_authorized_keys:
  - your-ssh-pubkey-line-goes-here
runcmd:
  - [ ip, addr, flush, dev, ens2 ]
  - [ ip, addr, add, 192.168.255.10/24, dev, ens2 ]
  - [ ip, link, set, dev, ens2, up ]
  - [ ip, route, add, default, via, 192.168.255.1 ]



Once online, ping google first then:

sudo apt update && \
sudo apt install -y python3 python3-pip ansible openssh-client && \
pip3 install ncclient requests netmiko urllib3

python3 -m pip install ncclient requests netmiko urllib3 --break-system-packages
python3 -m pip install ansible-pylibssh --break-system-packages

sudo apt update && sudo apt install -y git

Next connect to github, use the key shown above:
ssh-keygen -t ed25519 -C "kmcdermott310@gmail.com"
cat ~/.ssh/id_ed25519.pub

Log into GitHub.
Go to Settings (click your profile icon in the top right).
Click SSH and GPG keys in the left sidebar.
Click New SSH key.
Give it a Title (e.g., "CML Ubuntu Node") and paste your key into the Key box.
Click Add SSH key.

cisco@cisco:~$ git clone git@github.com:kmcdermott310-droid/cml-lab.git

Then do "git pull" to keep pulling 



