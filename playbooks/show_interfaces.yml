---
- name: Show interfaces
  hosts: routers
  gather_facts: no
  tasks:
    - name: Run 'show ip int brief'
      cisco.ios.ios_command:
        commands:
          - show ip int brief
      register: result

    - name: Print output clearly
      debug:
        msg: |
          {{ item }}
      loop: "{{ result.stdout }}"
